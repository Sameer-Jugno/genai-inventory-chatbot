# Runtime deps for local scripts (create_opensearch_index, scraper is stdlib-only).
# Lambda zip installs these via scripts/build_ingestion_package.sh.
pydantic>=2,<3
opensearch-py>=2.4,<3
requests-aws4auth>=1.2,<2
pdfplumber>=0.11,<0.12
boto3>=1.34,<2
openpyxl>=3.1,<4
pypdf>=6,<7
